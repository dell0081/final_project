import 'package:flutter/material.dart';

class FlightsListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flights List'),
      ),
      body: Center(
        child: Text('This is the Flights List Page'),
      ),
    );
  }
}
